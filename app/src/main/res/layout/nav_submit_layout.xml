<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <EditText
        android:id="@+id/etRestoLocation"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etRestoName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="23dp"
        android:ems="10"
        android:hint="Input Restaurant Location"
        android:inputType="textPersonName" />

    <EditText
        android:id="@+id/etRestoName"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/etRestoLocation"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/etRestoLocation"
        android:layout_marginTop="37dp"
        android:ems="10"
        android:hint="Input Restaurant Name"
        android:inputType="textPersonName" />

    <Button
        android:id="@+id/bSubmitResto"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etRestoLocation"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="29dp"
        android:background="@color/colorButtons"
        android:text="SUBMIT" />
</RelativeLayout>